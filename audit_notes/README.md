# Audit Notes

This folder holds generated audit trails for the school lunch modernization grants workflow.

Instructor fallback commands:

```bash
python scripts/classify_cafeteria_partner_roles.py
python scripts/build_school_year_panel.py
```

Expected generated notes:

- `cafeteria_partner_role_classification_audit.md`
- `build_school_year_panel_audit.md`
